pub const TI_DURATION: std::time::Duration = std::time::Duration::from_micros(1_000_000 / 1024);
pub const XTI_DURATION: std::time::Duration =
    std::time::Duration::from_micros(1_000_000 / (16 * 1024));

pub const XTI_PER_TI: u32 = 16;
